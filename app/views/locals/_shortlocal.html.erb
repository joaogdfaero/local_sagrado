<!DOCTYPE html>
<html>
<head>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css">
</head>
<body class="bg-gray-100">
  <div id="<%= dom_id local %>" class="bg-gray-100 p-4 shadow-md rounded-md mb-3">
    <div class="container mx-auto mt-5 mb-10 max-w-screen-lg flex">
      <div class="flex-1 mr-8">
        <div>
          <h1 class="text-2xl font-bold text-gray-800 mb-5">
            <%= local.nome %>
          </h1>
        </div>

        <div class="mb-2">
          <p>
            <strong class="text-yellow-800">Endereço</strong><br>
            <%= local.endereco %>
          </p>
        </div>

        <div class="mb-2">
          <p>
            <strong class="text-yellow-800">Religião</strong><br>
            <%= local.religiao_denominacao %>
          </p>
        </div>

      <%= link_to "Abrir no Maps", "https://www.google.com/maps/search/?api=1&query=#{CGI.escape(local.endereco)}", target: "_blank", style: "font-size: 0.875rem; color: #3490dc; text-decoration: none; hover: #2779bd;" %>

        <div class="mb-2 hidden">
          <p>
            <strong class="text-yellow-800">Tipo</strong><br>
            <%= local.tipo %>
          </p>
        </div>
      </div>

      <div class="flex-1">
        <div class="mb-2">
          <p>
            <strong class="text-yellow-800">Descrição</strong><br>
            <%= truncate(local.descricao, length: 200, separator: ' ', omission: '...') %>
          </p>
        </div>

        <div class="mb-2">
          <p>
            <strong class="text-yellow-800">Eventos</strong><br>
            <%= truncate(local.eventos, length: 200, separator: ' ', omission: '...') %>
          </p>
        </div>

        <div class="mt-4">
          <%= link_to "Ver detalhes", local, class: "text-blue-500 hover:text-blue-700" %>
        </div>
      </div>
    </div>
  </div>
</body>



